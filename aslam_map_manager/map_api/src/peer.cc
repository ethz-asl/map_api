#include "map-api/peer.h"

namespace map_api {

bool Peer::request(const proto::HubMessage& request,
               proto::HubMessage* response) {
  // TODO(tcies) implement
  return false;
}

} // namespace map_api
