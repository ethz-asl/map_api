package map_api.proto;

message RoutedChordRequest {
  optional string table_name = 1;
  optional bytes serialized_message = 2;
}

message PeerList {
  repeated string peers = 1;
}

message ChunkList {
  repeated string chunk_ids = 1;  // TODO(tcies) evt. use Id Message type
}